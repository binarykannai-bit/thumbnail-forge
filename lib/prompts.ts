import { getVisualUpload } from './constants';

export function buildPrompt(data: any) {
  const refImageCount = data.refs.length;
  const assetCount = data.visualAssets.length;
  const upload = getVisualUpload(data.mainVisual);

  let imageInstructions = '';
  if (refImageCount > 0 || assetCount > 0) {
    imageInstructions = '\n## 添付画像の解釈\n';
    if (refImageCount > 0) {
      imageInstructions += `- 最初の${refImageCount}枚: 参考サムネ画像\n`;
      data.refs.forEach((r: any, i: number) => {
        imageInstructions += `  - 画像${i+1}${r.isMain ? '(★メイン)' : ''}: ${r.context}\n`;
      });
    }
    if (assetCount > 0 && upload) {
      imageInstructions += `- 続く${assetCount}枚: ${upload.label}（メインビジュアル「${data.mainVisual}」用の素材）\n`;
      // Visual-type-specific instructions
      if (data.mainVisual.includes('顔出し') || data.mainVisual.includes('複数人物')) {
        imageInstructions += `  → サムネ生成プロンプトでは、これらの人物の特徴(髪型・表情の傾向・服装・年齢層・体型・雰囲気・肌の特徴など)を文字で詳細に描写し、Image 2.0が同じイメージの人物を生成できるようにすること。**人物の同一性を保つこと**が最優先。\n`;
      } else if (data.mainVisual.includes('キャラクター・アバター')) {
        imageInstructions += `  → このキャラクター・アバターのデザイン特徴(髪型・髪色・目の色・服装・小物・表情の傾向・スタイル)を文字で詳細に描写し、Image 2.0が同じキャラクターを生成できるようにすること。**キャラの一貫性**を保つこと。\n`;
      } else if (data.mainVisual.includes('図解・グラフ')) {
        imageInstructions += `  → これらの図解・グラフのスタイル(配色・ライン・タイポグラフィ・データ表現方法)を参考に、サムネに最適化した図解要素を生成プロンプトに組み込むこと。\n`;
      } else if (data.mainVisual.includes('実物・物体')) {
        imageInstructions += `  → これらの物体の特徴(形状・色・質感・ブランド要素)を文字で詳細に描写し、サムネで同じ実物が映るようにプロンプト化すること。\n`;
      } else if (data.mainVisual.includes('風景・場所')) {
        imageInstructions += `  → これらのロケーションの特徴(時間帯・天候・光・建物・自然要素)を文字で描写し、サムネに反映すること。\n`;
      } else if (data.mainVisual.includes('シーンキャプチャ')) {
        imageInstructions += `  → これらのシーンの構図・人物配置・アクションを参考にし、最も衝撃的な瞬間をサムネとして抽出する構成にすること。\n`;
      } else if (data.mainVisual.includes('比較・並列')) {
        imageInstructions += `  → これら2つの対象を左右(またはbefore/after)で並列配置し、対比が明確に分かるサムネを生成すること。\n`;
      }
    } else if (upload && !upload.required && assetCount === 0) {
      imageInstructions += `\n## 素材画像なし\nメインビジュアル「${data.mainVisual}」用の素材画像はアップロードされていません。AIが文脈から最適な素材を新規生成します。原稿内容と方向性から最も訴求力のある${data.mainVisual}を創造してください。\n`;
    }
  } else if (upload && !upload.required) {
    imageInstructions = `\n## 素材画像なし\nメインビジュアル「${data.mainVisual}」用の素材画像はアップロードされていません。AIが文脈から最適な素材を新規生成します。原稿内容と方向性から最も訴求力のある${data.mainVisual}を創造してください。\n`;
  }

  const personaSection = data.persona && data.persona.trim()
    ? `\n## 詳細ペルソナ（最重要・このペルソナの心に刺さるサムネ・タイトルを最優先で作る）\n\`\`\`\n${data.persona.trim()}\n\`\`\`\n\nこのペルソナが「これは自分のことだ」「自分が抱えている悩みそのものだ」と感じる瞬間を作ることが最優先タスクです。コピー選択・色彩・人物表情・構図、すべてこのペルソナの心理状態と境遇に最適化してください。一般論ではなく、このペルソナ固有の言語・関心事・痛みポイントを反映させること。`
    : '';

  const directions = data.direction;
  let directionInstruction = '';
  if (directions.length === 1) {
    directionInstruction = `ユーザー指定の方向性は「${directions[0]}」のみ。5パターンすべてこの方向性で異なる切り口（心理トリガー・構図・コピー戦略）を試してください。labelは「${directions[0]}型・○○」形式で差別化を明示。`;
  } else if (directions.length === 2) {
    directionInstruction = `ユーザー指定の方向性[${directions.join(', ')}]を以下のように分散:
- 1: ${directions[0]}型 / 2: ${directions[0]}型(別アプローチ) / 3: ${directions[1]}型 / 4: ${directions[1]}型(別アプローチ) / 5: ${directions[0]} × ${directions[1]} 融合型`;
  } else if (directions.length === 3) {
    directionInstruction = `ユーザー指定の方向性[${directions.join(', ')}]を以下のように分散:
- 1: ${directions[0]}型 / 2: ${directions[1]}型 / 3: ${directions[2]}型 / 4: ${directions[0]} × ${directions[1]} 融合型 / 5: ${directions[1]} × ${directions[2]} 融合型`;
  } else {
    const top5 = directions.slice(0, 5);
    directionInstruction = `ユーザー指定の方向性[${directions.join(', ')}]から以下を1つずつ: ${top5.map((d: string, i: number) => `${i+1}: ${d}型`).join(', ')}`;
  }

  return `あなたはYouTubeサムネイル戦略の最高峰の専門家です。視聴者心理学・CTR最適化・コピーライティングに精通し、特に日本のYouTube市場における「思わずクリックしてしまうサムネ」の文法を熟知しています。

# 重要：必ず守るべきYouTubeノウハウ

## サムネ作成ノウハウ（2026年最新版）

### 視認性の鉄則
- スマホで一瞬で目に止まる設計が必須（おすすめ欄でスクロール時1秒で判定される）
- 文字は3〜4語以内、太字ゴシック系
- 白文字＋黒フチ、または黄色文字＋濃色背景が王道
- 色は3色以内に絞る
- 文字に縁取り・影・半透明帯で視認性確保

### 2026年に効く5つの構図パターン
1. **ブラックボックス式**：答えを●●やモザイクで隠す（HOW TO・謎解き・ランキング系）
2. **4分割式**：複数シーンを並べて情報量演出（Vlog・まとめ・比較系）
3. **ビフォーアフター式**：変化前後を矢印・vsで（ダイエット・片付け系）
4. **比較式**：2つの選択肢を並べる（レビュー・検証系）
5. **顔アップ＋パワーワード式**：王道（エンタメ・解説系）

### 人物表現
- 人物入りはCTR 10〜20%超え可能、文字だけは1-5%止まり
- 表情と動画内容を一致させる（衝撃→驚き、簡単→安堵、警告→困惑）
- やや誇張した表情のほうがサムネ上では伝わる
- 視線を合わせるか対象物を見つめる構図

### 色彩戦略
- 赤：緊急・警告・情熱（強い訴求）
- 黄：注目・パワーワード背景に最適
- 青/ネイビー：信頼・知性・教育/ビジネス系
- 緑：安心・健康・自然
- 黒：権威・高級感・対比・縁取り

### パワーワードのパターン
- 希少性：「9割が知らない」「限定公開」「誰も教えてくれない」
- 損失回避：「知らないと損」「やってはいけない」「失敗する人の共通点」
- 権威性：「プロが教える」「専門家の本音」「医師が警告」
- 数字具体性：「3つの法則」「月5万」「最短7日」
- 意外性：「実は逆効果」「衝撃の真実」「常識が覆る」
- 緊急性：「今すぐ」「最新」「2026年版」

### 「強フック」型の作り方（指定された場合の特別指示）
強フック型は、**スクロールを物理的に止めさせる衝撃の一撃**を狙う構図。以下を必ず採用:
- 結論を冒頭で見せる手法（「●●すると死にます」「全員クビ」など断定形）
- 数字＋衝撃事実の組合せ（「99%が知らない」「年収が3倍に」「たった3秒で」）
- 否定×逆説（「努力するな」「貯金は危険」「英語勉強は無駄」）
- タブー領域への踏込（「言ってはいけない」「業界の闇」「禁断の」）
- 表情は「驚愕」「困惑」「絶望」など強感情、絶対に笑顔にしない
- 色は赤×黒×黄の高コントラスト3色厳守
- メイン文字は最大級のサイズで画面の40-50%を占める

### 「強キラーワード」型の作り方（指定された場合の特別指示）
強キラーワード型は、**1つのパワーワードがクリックを決定づける**構図。以下を必ず採用:
- 単語1つで意味が完結するキラーワードを中央に巨大配置（例: 「終了」「廃止」「危険」「告発」「絶望」「真実」「禁止」「暴露」「逆転」「最後」）
- そのワードを画面中央60-70%占有で配置、視認性最優先
- 補助文字は最小限（8文字以内のサブテキスト1つまで）
- 黄色の背景にキラーワード黒文字、または黒背景に赤文字の配色が王道
- 人物がいる場合はキラーワードを指差す/驚いて見る構図
- フォントは極太ゴシックの最大級ウェイト
- 装飾は最小限、ワード自体の威力で勝負

### 視聴者の興味を引く設計（最重要）
- 「見たときに結果が分からない」が原則
- サムネ＝Q（質問）、動画＝A（答え）の関係性
- 答えを部分的に隠す / 意外な対比 / タブー匂わせ / 変化の前後示唆

### 文字配置
- 左上または右上に最重要情報（視線の起点）
- 人物の顔を隠さない位置にテキスト
- Z/Fの法則を意識した視線誘導

### クリックベイトNG（2026年規制強化）
- 動画内容と乖離させない（AIが満足度アンケートで検出）
- 誇張しすぎず、しょぼく見せすぎない
- 動画の核心を正直にサムネで表現

## タイトル作成ノウハウ

### 構造の基本
「検索ワード」＋「数字」＋「感情・興味をそそる表現」
例：「副業 月30万｜たった1日10分でできる方法とは」

### 必須要素（必ず守る）
- 検索キーワードを先頭に配置（AIが内容を理解し関連動画に表示するため）
- 数字を必ず1つ以上含める（具体性・信ぴょう性UP）
- 疑問形 or 限定感 or 感情訴求 or 権威性のいずれかを含める

### NGパターン
- 抽象的すぎる（「在宅副業」「初心者向け」「便利な使い方」）
- 検索キーワードが冒頭にない
- 数字なし

### 改善例
- ❌「在宅副業」→ ⭕「在宅副業で月5万｜主婦でもできた3つの方法」
- ❌「初心者向けの方法」→ ⭕「初心者が最短で稼ぐ方法｜たった1日で収益化」
- ❌「便利な使い方」→ ⭕「実は99%の人が知らない便利な使い方5選」

### 2026年特有の制約
- タイトルとサムネと動画本編の整合性が必要（AIが乖離検出）
- クリックベイトは満足度アンケートで検出されペナルティ
- 共有先で読まれたときに説明的になる設計

---

# 入力情報

## 動画情報
- メインカテゴリ: ${data.mainCategory}${data.subCategory ? `\n- サブカテゴリ: ${data.subCategory}` : ''}
- ターゲット視聴者(属性): ${data.audiences.join(', ')}
- 視聴者の心理状態: ${data.psychology.length > 0 ? data.psychology.join(', ') : '指定なし(原稿から判断)'}
- サムネ方向性: ${data.direction.join(', ')}
- サムネのメインビジュアル: ${data.mainVisual}
- デザイン密度: ${data.density}
- 文字サイズ感: ${data.fontSize}
- NG表現: ${data.ng.length > 0 ? data.ng.join(', ') : 'なし'}
${personaSection}
${imageInstructions}

## 動画原稿・台本・メモ
\`\`\`
${data.script}
\`\`\`

## 必須文言
${data.requiredPhrases.length > 0 ? data.requiredPhrases.map(p => `- 「${p}」`).join('\n') : 'なし'}

# 🔴 絶対ルール1: 原稿の徹底活用（最優先）
タイトル・サムネ文字案を作成する際、上記の「サムネ作成ノウハウ」と「タイトル作成ノウハウ」を**参照基準**として、以下を絶対に守ってください:

1. **原稿から「タイトル作成ノウハウ」の必須要素を抽出すること**:
   - 原稿に登場する **検索キーワード候補**（ジャンル名・固有名詞・サービス名・商品名など）→ タイトル先頭配置用に使う
   - 原稿に登場する **具体的な数字**（金額・期間・人数・割合など）→ タイトルとサムネ文字に組み込む
   - 原稿の中で最も強い **感情訴求ポイント**（驚き・損失・権威など）→ パワーワード化する

2. **原稿から「サムネ作成ノウハウ」のパワーワード6パターンに該当する表現を発掘すること**:
   - 希少性・損失回避・権威性・数字具体性・意外性・緊急性の6パターンのうち、原稿に最も適合するものを選び、原稿内の表現と結びつけること
   - 例: 原稿に「ほとんどの人が見落としている」とあれば → 希少性パターン「9割が知らない」系に変換
   - 例: 原稿に「失敗例」とあれば → 損失回避パターン「失敗する人の共通点」系に変換

3. **原稿に書かれていない数字・事実・主張を創作しないこと**（2026年クリックベイト規制で検出されるため）

4. **原稿の語彙・表現を最優先で活用**し、一般論や常套句で逃げないこと。原稿が抽象的・短い場合のみ、原稿の方向性を尊重した上で補完すること

# 🔴 絶対ルール2: 参考画像の活用
${refImageCount > 0 ? `参考サムネ画像が${refImageCount}枚アップロードされています。これらの画像から以下を読み取り、5つのサムネ案のImage 2.0プロンプトに**参考として反映**してください:
- 配色パターン（ベース色・アクセント色・文字色）の傾向
- 構図（人物配置、文字配置、視線方向）の傾向
- フォントの太さ・スタイルの傾向
- 全体の雰囲気・トーン

ただし、**完全に同じデザインを再現するのは避け**、参考のエッセンスを取り入れつつ独自性のあるサムネに仕上げてください（パクリ・著作権侵害を避けるため）。特に★メイン参考の画像は最も重視するが、踏襲しすぎず参考レベルに留めること。` : '参考サムネ画像はアップロードされていません。'}

${assetCount > 0 ? `\nまた、メインビジュアル素材が${assetCount}枚あります。これらに映る要素（人物の顔・キャラ・物体・図解等）の特徴を**詳細に文字描写**し、Image 2.0が同じ要素を再現できるレベルで指定してください。` : ''}

# 🔴 重要な前処理タスク（出力前に必ず実行）
原稿を熟読し、以下を**自動抽出**してください（出力には含めない、内部処理のみ）：

1. **最大のフック**: 視聴者が思わず食いつく「驚き」「衝撃」「ギャップ」のポイント1つを原稿から抽出
2. **動画の結論**: 視聴者が最終的に得る学び・行動指針
3. **検索キーワード候補**: タイトルの先頭に置くべき検索ワード（メインカテゴリ・サブカテゴリ・原稿の固有名詞から）
4. **心理トリガー候補**: 好奇心ギャップ/恐怖訴求/損失回避/逆張り/権威付け/共感のうち最強のもの
5. **使える具体的数字・固有名詞リスト**: 原稿の中に登場する数字・人名・サービス名・地名・時期などをリスト化（タイトル・サムネに必ず使うため）
${data.direction.includes('強フック') ? `
6. **強フック抽出（強フック型を作るため必須）**:
   - 原稿の中で最も衝撃的な事実1つ（数字付きが望ましい）
   - 原稿の中で最も意外な矛盾・パラドックス・逆説1つ
   - 原稿の中でタブー視されがち or 業界の常識に反する主張1つ
   - これらを「強フック型」サムネのコピーの核として採用すること
` : ''}${data.direction.includes('強キラーワード') ? `
7. **強キラーワード抽出（強キラーワード型を作るため必須）**:
   - 原稿の中で最も強い意味を持つ単語1つを抽出（漢字熟語2-4文字推奨。例: 「終了」「廃止」「危険」「告発」「絶望」「真実」「禁止」「暴露」「逆転」「最後」「完敗」「警告」など）
   - 該当する強ワードが原稿内に明示的にない場合のみ、原稿の核心を1単語で象徴する強い漢字熟語を新規生成（ただし原稿の主旨と整合させること）
   - そのワードを「強キラーワード型」サムネの中央に巨大配置する設計にすること
` : ''}

# 方向性の割り振り
${directionInstruction}

# 🔴 ノウハウ遵守の自己検証（出力前に必ず確認）
生成した3つのタイトル案・5つのサムネ案について、以下を**1つずつ**自己検証してください:

**タイトル案チェック:**
- ✅ 各タイトルが「検索キーワード（先頭）+数字+感情訴求」の構造を満たしているか
- ✅ 数字を必ず1つ以上含んでいるか
- ✅ 原稿の固有名詞・事実を1つ以上活用しているか
- ✅ 3つの心理トリガーが互いに異なるか

**サムネ案チェック:**
- ✅ thumbnailText.mainが12文字以内か（厳守）
- ✅ 5案が互いに明確に差別化されているか
- ✅ 視聴者の心理状態に対応する表情・色彩がimagePromptに明記されているか
- ✅ 強フック型を含む場合: 断定形/否定逆説/タブーのいずれかの要素が入っているか
- ✅ 強キラーワード型を含む場合: 1単語が画面中央60-70%占有の指定があるか
- ✅ 各imagePromptに「視認性の鉄則」（白文字+黒フチ等）「色彩戦略」（赤/黄/青/黒の使い分け）が明記されているか
- ✅ 参考画像がある場合、配色・構図がプロンプトに反映されているか

これらをすべて満たさない案は**作り直してから**出力してください。

# あなたのタスク
以下のJSON形式で出力してください。**JSON以外の説明文・前置き・コードブロック記号(\`\`\`)は一切不要、JSONのみを出力してください**。

{
  "titles": [
    {
      "text": "タイトル案。【検索キーワード+数字+感情訴求】の構造を必ず守ること。40文字以内。",
      "trigger": "好奇心ギャップ|恐怖訴求|損失回避|逆張り|権威付け|共感",
      "expectedCTR": "高|中高|中"
    }
  ],
  "thumbnails": [
    {
      "label": "方向性ラベル(例: 強インパクト型・恐怖訴求 / 信頼感×権威性 融合型)",
      "thumbnailText": {
        "main": "メイン文字(8〜12文字、Image 2.0で確実に描画できる短さ)",
        "sub": "サブ文字(8文字以内、不要なら空文字)",
        "marker": "黄色マーカーで強調する1単語"
      },
      "imagePrompt": "ChatGPT Image 2.0用の詳細な英語プロンプト(500語以上)。**サムネは必ずYouTube標準の1280x720(16:9 widescreen aspect ratio)で構図を組むこと。重要な要素は中央60%エリアに収め、左右上下が切れても致命的にならない構図にすること(API側で1536x1024生成→1280x720中央クロップが行われる)**。日本語テキストは必ずダブルクォートで囲んで指定。上記の【視認性の鉄則】【5つの構図パターン】【人物表現】【色彩戦略】【文字配置】を必ず反映させること。具体的に: (1)選択した構図パターン名を明示 (2)背景・人物・ライティング・配色 (3)文字配置(左上/右上/中央/中央60%エリア)とフォントスタイル(太字ゴシック・白文字+黒フチ等) (4)視線誘導 (5)感情を表現する具体的表情。アップロードされた人物画像がある場合は、その人物の特徴(髪型・服装・年齢層・雰囲気)を文字で詳細に描写しイメージを再現させること。参考サムネ画像があれば色味・構図を反映。"
    }
  ]
}

# 重要な制約（必ず遵守）
- **titlesは必ず3つ**。心理トリガーを3つ全部違うものにし、構造ルール（検索ワード+数字+感情訴求）を全て守ること
- **thumbnailsは必ず5つ**。上記「方向性の割り振り」に厳密に従うこと
- 各サムネのlabelは「○○型・△△」「○○×△△ 融合型」のいずれかの形式
- thumbnailText.mainは必ず12文字以内（Image 2.0の日本語描画限界）
- imagePromptは英語で書き、日本語テキストのみ"〜"で囲んで指定する。上記ノウハウを必ず反映
- NG表現に指定された要素は絶対に避けること
- 必須文言が指定されている場合、5つのサムネ文字案のうち少なくとも3つに含めること
- 5つのサムネ案は互いに明確に差別化すること（同じ構図・コピーにならないこと）
- 視聴者の心理状態が指定されている場合、その感情トリガーを直接刺激する表情・色彩・コピーを必ず使用すること
- **原稿の固有名詞・数字・事実を各案で必ず1つ以上活用すること**（一般論で逃げない）
- **強フック型が指定された場合**: 原稿から抽出した「最も衝撃的な事実」「最も意外な矛盾」「タブー的主張」のいずれかをコピーの核として必ず使うこと
- **強キラーワード型が指定された場合**: 原稿から抽出した「最も強い1単語（漢字熟語推奨）」を画面中央60-70%占有で配置するよう必ずプロンプト指定すること
- 動画内容と乖離するクリックベイトは禁止（2026年は満足度アンケートで検出される）`;
}
