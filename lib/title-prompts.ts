import { extractNumbersFromScript, type ExtractedNumbers } from './script-numbers';

/**
 * タイトル生成専用の軽量プロンプト。
 *
 * 設計方針:
 * - サムネ生成と並列実行するため、最小限のノウハウだけ含める
 * - gpt-5-mini など軽量モデルでも品質が出るよう、簡潔・明示的に
 * - 1264行のメインprompts.tsから「タイトル関連のエッセンス」だけ抽出
 *
 * 推定速度: gpt-5-mini で 5〜10秒（メインのgpt-5 30〜60秒に対して大幅短縮）
 */

export interface TitlePromptData {
  script: string;
  mainCategory?: string;
  subCategory?: string;
  audiences?: string[];
  psychology?: string[];
  direction?: string[];
  persona?: string;
  requiredPhrases?: string[];
  ng?: string[];
}

export function buildTitlePrompt(data: TitlePromptData): string {
  const extracted = extractNumbersFromScript(data.script || '');
  const numbersSection = buildNumbersSectionForTitle(extracted);
  const directions = data.direction || [];
  const isStrongType = directions.some((d) =>
    ['強インパクト', '強フック', '強キラーワード'].includes(d)
  );

  return `あなたはYouTubeタイトル戦略の最高峰の専門家です。日本のYouTube市場における視聴者心理学・CTR最適化・コピーライティングに精通しています。

# 🎯 タスク
動画原稿から、**3つの完全に異なる訴求軸**でタイトル案を生成してください。

${numbersSection}

# 入力情報

## 動画情報
- メインカテゴリ: ${data.mainCategory || '指定なし'}${data.subCategory ? `\n- サブカテゴリ: ${data.subCategory}` : ''}
- ターゲット視聴者: ${data.audiences && data.audiences.length > 0 ? data.audiences.join(', ') : '指定なし'}
- 視聴者心理: ${data.psychology && data.psychology.length > 0 ? data.psychology.join(', ') : '指定なし(原稿から判断)'}
- 方向性: ${directions.length > 0 ? directions.join(', ') : '指定なし'}
${data.persona ? `- ペルソナ詳細: ${data.persona}` : ''}
- NG表現: ${data.ng && data.ng.length > 0 ? data.ng.join(', ') : 'なし'}

## 動画原稿
\`\`\`
${data.script}
\`\`\`

${data.requiredPhrases && data.requiredPhrases.length > 0 ? `## 必須文言（タイトル内に必ず含めること）
${data.requiredPhrases.map((p) => `- 「${p}」`).join('\n')}
` : ''}

# 🔑 タイトル作成の絶対ルール

## 1. 基本構造（必須）
「**検索キーワード**」+「**数字**」+「**感情・興味をそそる表現**」

- 検索キーワードは40文字以内のどこかに必ず含める（先頭固定ではない）
- 数字を必ず1つ以上含める（原稿に登場する数字のみ使用可、リスト外NG）
- 感情訴求を必ず含める（驚き・損失回避・好奇心・利益等）

## 2. 5つのトリガー型（3案で全部違う型を使う）

**型1: 知らないと損型** — 【】記法 + 警告調
- 構造: 「【知らないと損】[テーマ]の[数字]NG」
- 例: 「【知らないと損】NISAでやってはいけない3つのこと」

**型2: 驚き＋利益型** — 数字を冒頭/メインに、利益強調
- 構造: 「[衝撃数字]で[得られる利益]｜[補足条件]」
- 例: 「月5万円浮く家計術｜会社員でもできる7つの習慣」

**型3: 共感＋逆転型** — 共感フレーズで冒頭、逆転の事実を後半に
- 構造: 「[共感する状況]だった私が[逆転の結果]」「[よくある誤解]じゃない。本当の[答え]」
- 例: 「貯金0円だった私が月10万稼げた理由｜誰でも真似できる4ステップ」

**型4: 結論先出し型** — 「結論」「答え」を最初に
- 構造: 「【結論】[テーマ]で大事なのは[数字]つだけ」
- 例: 「【結論】副業で稼ぐ正解は、たった1つだけです」

**型5: 問題提起型** — 疑問形・なぜ・本当に
- 構造: 「なぜ[問題]？答えは[数字]つの[原因]」「本当に[一般通念]？実は[真実]」
- 例: 「なぜあなたのnoteは売れない？答えは4つのNG」

## 3. 構造の多様性ルール（重要）

**3つのタイトルが全部「○○｜△△」のような同じ記法・同じ構造になるのは絶対NG**。
- 型1の【】記法 / 型2の冒頭数字 / 型3の共感フレーズ / 型4の【結論】 / 型5の疑問形
- 各型固有の構造を採用し、3つで明確に差別化すること

## 4. 文字数
- **40文字以内厳守**（YouTubeのモバイル表示限界）
- 検索キーワード+数字+感情訴求を40文字に収めるため、無駄な装飾語は削る

${isStrongType ? `## 5. 🔥【強○○型 選択時の必須】パワーワード含有 🔥

ユーザーは「${directions.filter(d => ['強インパクト', '強フック', '強キラーワード'].includes(d)).join('・')}」を選択しています。3つのタイトルのうち**少なくとも2つ**に、以下のいずれかのパワーワードを必ず含めてください:

1. **感情爆発系**: マジで / ヤバい / ヤバすぎ / ガチで / えぐい / 鬼ヤバ
2. **緊急系**: 緊急 / 速報 / 今すぐ / 手遅れ / 最終警告 / 全員逃げて
3. **終焉系**: 終了 / 終わった / オワコン / 全滅 / 崩壊 / もう無理
4. **驚愕系**: え？ / は？ / ウソ / マジ？ / 待って / 信じられない
5. **断定系**: 確定 / 決定 / 100% / 全員必見 / 絶対に / 完全に
6. **衝撃数値系**: 99%が知らない / 100万消失 / たった3秒 / 月収0→100万
7. **タブー系**: 国が隠してる / 業界の闇 / 言ってはいけない / 禁断の / 暴露 / 告発

### 強○○型で絶対NGの語彙
- ❌ 「真相」「本質」「核心」「構造」「仕組み」「裏側」「分岐点」（抽象的・冷静系）
- ❌ 「解剖」「考察」「分析」「検証」「論考」（中立すぎる）
- ❌ 古い慣用句（「焼け石に水」「時間稼ぎ」「虚を突く」「機先を制する」）
- ❌ 「策」「論」「説」「式」「観」などの固い漢字単独使用

` : ''}

# 🚨 日本語品質の絶対NG

## NG1: 主語不明・抽象的すぎ
- ❌ 「止まらない理由」（何が止まらない？）
- ❌ 「ドル円160円の裏側」（裏側とは？）
- ❌ 「6月16日が分岐点」（何の分岐点？）
- ✅ 「円安が止まらない3つの理由」「ドル円160円なぜ介入なし」

## NG2: 古い慣用句・難解な比喩
- ❌ 「焼け石に水の介入」
- ❌ 「時間稼ぎの政策」
- ❌ 「虚を突く戦略」
- → 中学生でも3秒で意味が取れる平易な言葉に置き換える

## NG3: 抽象漢語動詞
- ❌ 「台頭する」「凋落する」「乖離する」「逼迫する」
- ✅ 「増える」「落ちる」「ずれる」「やばい」

## NG4: 漢語連結で意味崩壊
- ❌ 「焼け石に水の介入」「警戒級の崩壊」「構造から解剖」
- → 修飾関係を確認、声に出して読んで確認

## NG5: 未完成日本語
- ❌ 「日銀で割れ、円と積立は」（助詞「は」で途中終了）
- ✅ 文として完結させること（体言止めもOK）

# 🎯 3つのタイトル＝3つの異なる訴求軸

各タイトルは以下のように**完全に異なる切り口**で：
- title 1: 例えば「数字訴求型」（具体数字で得られる利益を見せる）
- title 2: 例えば「損失回避型」（知らないと損する系）
- title 3: 例えば「逆説・共感型」（よくある誤解を覆す）

→ 3つ並べたとき、視聴者に「全部違う動画」と感じさせる多様性が必要

# 出力形式

以下のJSON形式で**JSONのみ**を出力してください（前置き・コードブロック\`\`\`不要）:

\`\`\`json
{
  "titles": [
    {
      "text": "タイトル本文（40文字以内）",
      "trigger": "知らないと損型 | 驚き＋利益型 | 共感＋逆転型 | 結論先出し型 | 問題提起型",
      "structure": "検索ワード:○○ / 数字:○○ / 感情訴求:○○",
      "expectedCTR": "高 | 中高 | 中"
    }
  ]
}
\`\`\`

# 最終チェック（出力前に必ず自問）

- ☑ 3つのタイトルが**完全に異なる構造・トリガー型**を使っているか？
- ☑ 各タイトルが**40文字以内**か？
- ☑ 数字は**全て原稿に登場するもの**か？（リスト外の数字を作っていないか）
- ☑ 検索キーワードが**40文字以内のどこか**に含まれているか？
- ☑ 「真相」「本質」「裏側」「分岐点」「焼け石に水」などのNG語彙を**使っていない**か？
${isStrongType ? `- ☑ 強○○型のパワーワード（マジで/ヤバい/緊急/オワコン等）が**3案中2案以上**に含まれているか？` : ''}
- ☑ 主語が明確で、中学生が3秒で意味を取れるか？
`;
}

/**
 * タイトル用の数字ホワイトリストセクション（軽量版）。
 */
function buildNumbersSectionForTitle(extracted: ExtractedNumbers): string {
  if (!extracted || extracted.all.length === 0) {
    return `# 🔢 数字の使用制約

**原稿から数字が抽出できませんでした。タイトルに具体的な数字を一切使用しないでください。**`;
  }

  return `# 🔢 タイトルで使用可能な数字（ホワイトリスト）

**原稿から抽出された下記の数字のみ**をタイトル内で使用してください:

${extracted.all.map((n) => `「${n}」`).join('、')}

**リスト外の数字を1つでも使うのは絶対NG**。リストに無い金額・人数・割合・期間などを勝手に作り出してはいけません。`;
}
